export enum HttpMethod {
  POST = 'POST',
  DELETE = 'DELETE',
  PUT = 'PUT',
  PATCH = 'PATCH',
  GET = 'GET'
}
export enum MenuType {
  DIRECTORY = 1,
  MENU = 2,
  BUTTON = 3
}

export enum Path {
  INDEX = '/index'
}

export enum Sex {
  MALE = 1,
  FEMALE = 2
}

export enum Whether {
  Y = 1,
  N = 0
}
